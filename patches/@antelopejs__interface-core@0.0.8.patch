diff --git a/dist/index.d.ts b/dist/index.d.ts
index 695f68323d11e77f15983c89fa6c99e9c1cb7062..1f5439e612bba4e3a0692b960da661eae61330db 100644
--- a/dist/index.d.ts
+++ b/dist/index.d.ts
@@ -27,6 +27,8 @@ type Func<A extends any[] = any[], R = any> = (...args: A) => R;
  * @returns A function that proxies calls to the implementation when available
  */
 export declare function InterfaceFunction<T extends Func = Func, R = Awaited<ReturnType<T>>>(identity?: string): (...args: Parameters<T>) => Promise<R>;
+/** Assigns deterministic identities to anonymous proxies in an interface declaration. */
+export declare function BindInterfaceProxyIdentities<T>(declaration: T, namespace: string): T;
 type RID<T> = T extends (id: infer P, ...args: any[]) => void ? P : never;
 type InterfaceImplType<T> = T extends RegisteringProxy<infer P> ? {
     register: P;
@@ -59,6 +61,8 @@ export declare function ImplementInterface<T extends Record<string, unknown>, T2
 interface InterfaceConnection {
     id?: string;
     path: string;
+    provider: string;
+    selected: boolean;
 }
 /**
  * Gets all instances of a specific interface across the system.
diff --git a/dist/index.js b/dist/index.js
index 76c00b9e791b851649474565cc15c78a60d247f3..c4604925e5d727453534256d4a909a1fe1caa2cc 100644
--- a/dist/index.js
+++ b/dist/index.js
@@ -17,6 +17,7 @@ Object.defineProperty(exports, "__esModule", { value: true });
 exports.RegisteringProxy = exports.IsInterfaceProxy = exports.GetResponsibleModule = exports.GetInterfaceProxyIdentity = exports.EventProxy = exports.AsyncProxy = void 0;
 exports.GetMetadata = GetMetadata;
 exports.InterfaceFunction = InterfaceFunction;
+exports.BindInterfaceProxyIdentities = BindInterfaceProxyIdentities;
 exports.ImplementInterface = ImplementInterface;
 exports.GetInterfaceInstances = GetInterfaceInstances;
 exports.GetInterfaceInstance = GetInterfaceInstance;
@@ -91,6 +92,48 @@ function InterfaceFunction(identity) {
     func.proxy = proxy;
     return func;
 }
+const PROXY_BRAND = Symbol.for("@antelopejs/interface-core/proxy");
+function bindProxyIdentity(proxy, identity) {
+    const brand = proxy[PROXY_BRAND];
+    if (!brand?.identity.includes(":anonymous:")) {
+        return;
+    }
+    const targetIdentity = `${brand.kind}:${identity}`;
+    const currentState = internal_1.internal.proxyStates.get(brand.identity);
+    const targetState = internal_1.internal.proxyStates.get(targetIdentity);
+    const state = targetState ?? currentState;
+    if (!state) {
+        throw new Error(`Interface proxy ${brand.identity} has no shared runtime state.`);
+    }
+    internal_1.internal.proxyStates.set(targetIdentity, state);
+    proxy[PROXY_BRAND] = Object.freeze({ ...brand, identity: targetIdentity });
+    proxy.state = state.value;
+}
+function BindInterfaceProxyIdentities(declaration, namespace) {
+    if (!namespace) {
+        throw new Error("Interface proxy identity namespace is required.");
+    }
+    const visited = new WeakSet();
+    const visit = (value, path) => {
+        if ((typeof value !== "object" && typeof value !== "function") || !value) {
+            return;
+        }
+        const proxy = typeof value === "function" && value.proxy ? value.proxy : value;
+        if ((0, proxies_1.IsInterfaceProxy)(proxy)) {
+            bindProxyIdentity(proxy, `${namespace}.${path}`);
+            return;
+        }
+        if (visited.has(value)) {
+            return;
+        }
+        visited.add(value);
+        for (const [key, child] of Object.entries(value)) {
+            visit(child, path ? `${path}.${key}` : key);
+        }
+    };
+    visit(declaration, "");
+    return declaration;
+}
 function isObject(value) {
     return typeof value === "object" && value !== null;
 }
diff --git a/dist/internal.d.ts b/dist/internal.d.ts
index cbadc37ecb4dda8ff465a3304a8451883a5901ac..3598aab117f709ab8f2c16e07dc9e91cdb5209e8 100644
--- a/dist/internal.d.ts
+++ b/dist/internal.d.ts
@@ -4,9 +4,12 @@ export declare const RUNTIME_SYMBOL: unique symbol;
 export interface InterfaceConnection {
     id?: string;
     path: string;
+    provider: string;
+    selected: boolean;
 }
 export interface ModuleExecutionContext {
     module: string;
+    owner?: string;
     provider?: string;
     providerRoutes?: Readonly<Record<string, string>>;
 }
diff --git a/dist/modules.js b/dist/modules.js
index 6ce2202da461104daff349ca7941a72f3476d16d..dc1f469589cd23de113ca8bf087042c5a6faa07f 100644
--- a/dist/modules.js
+++ b/dist/modules.js
@@ -75,14 +75,15 @@ function runCleanup(cleanup, module, operation) {
     }
 }
 Events.ModuleDestroyed.register((module) => {
-    for (const cleanup of internal_1.internal.knownAsync.get(module) ?? []) {
-        runCleanup(cleanup, module, "detach-async-provider");
+    const owner = internal_1.internal.executionContext.getStore()?.owner ?? module;
+    for (const cleanup of internal_1.internal.knownAsync.get(owner) ?? []) {
+        runCleanup(cleanup, owner, "detach-async-provider");
     }
-    internal_1.internal.knownAsync.delete(module);
-    for (const cleanup of internal_1.internal.knownRegisters.get(module) ?? []) {
-        runCleanup(cleanup, module, "detach-registering-provider");
+    internal_1.internal.knownAsync.delete(owner);
+    for (const cleanup of internal_1.internal.knownRegisters.get(owner) ?? []) {
+        runCleanup(cleanup, owner, "detach-registering-provider");
     }
-    internal_1.internal.knownRegisters.delete(module);
+    internal_1.internal.knownRegisters.delete(owner);
     for (const proxy of internal_1.internal.registeringProxies) {
         try {
             proxy.unregisterModule(module);
diff --git a/dist/proxies.js b/dist/proxies.js
index 10a504eba94b35358c4757b60eeb57b415ddb46e..0d2f1b6bf9d2d1d034e935bf70fd3941be5e3bb5 100644
--- a/dist/proxies.js
+++ b/dist/proxies.js
@@ -57,13 +57,20 @@ function GetInterfaceProxyIdentity(value) {
 function getAttachmentRoute(manualDetach) {
     const context = internal_1.internal.executionContext.getStore();
     const responsible = manualDetach || context?.module ? undefined : GetResponsibleModule();
-    const owner = context?.module ?? responsible ?? DEFAULT_PROVIDER;
+    const owner = context?.owner ?? context?.module ?? responsible ?? DEFAULT_PROVIDER;
     return { owner, provider: context?.provider ?? owner };
 }
 function getRequestedProvider(proxyIdentity) {
     const context = internal_1.internal.executionContext.getStore();
     return context?.providerRoutes?.[proxyIdentity] ?? context?.provider;
 }
+function bindProviderCallback(callback) {
+    const context = internal_1.internal.executionContext.getStore();
+    if (!callback || !context) {
+        return callback;
+    }
+    return (...args) => internal_1.internal.executionContext.run(context, () => callback(...args));
+}
 function selectProvider(callbacks, proxyIdentity, requested) {
     if (requested) {
         const callback = callbacks.get(requested);
@@ -100,13 +107,14 @@ class AsyncProxy {
     onCall(callback, manualDetach) {
         const route = getAttachmentRoute(manualDetach);
         const lease = { ...route, generation: internal_1.internal.nextLeaseGeneration++ };
-        this.state.callbacks.set(route.provider, { callback, ...lease });
+        const boundCallback = bindProviderCallback(callback);
+        this.state.callbacks.set(route.provider, { callback: boundCallback, ...lease });
         if (!manualDetach) {
             internal_1.internal.addAsyncProxy(route.owner, {
                 cleanup: () => this.detach(lease),
             });
         }
-        this.replayQueue(route.provider, callback);
+        this.replayQueue(route.provider, boundCallback);
         return lease;
     }
     /** Detaches one leased provider, or every provider when called without a lease. */
@@ -262,9 +270,11 @@ class RegisteringProxy {
     attachHandlers(register, unregister, manualDetach, shouldReplay = true, attachmentRoute) {
         const route = attachmentRoute ?? getAttachmentRoute(manualDetach);
         const lease = { ...route, generation: internal_1.internal.nextLeaseGeneration++ };
+        const boundRegister = bindProviderCallback(register);
+        const boundUnregister = bindProviderCallback(unregister);
         this.state.callbacks.set(route.provider, {
-            register,
-            unregister,
+            register: boundRegister,
+            unregister: boundUnregister,
             manualDetach: Boolean(manualDetach),
             ...lease,
         });
@@ -274,8 +284,8 @@ class RegisteringProxy {
                 unregisterModule: (module) => this.unregisterModule(module),
             });
         }
-        if (register && shouldReplay) {
-            this.replayRegistrations(route.provider, register);
+        if (boundRegister && shouldReplay) {
+            this.replayRegistrations(route.provider, boundRegister);
         }
         return lease;
     }
